# yaml-language-server: $schema=https://raw.githubusercontent.com/databricks/cli/main/bundle/schema/jsonschema.json
bundle:
  name: pokeapi-lakehouse
  databricks_cli_version: ">= 1.8.0"

include:
  - resources/*.yml

sync:
  include:
    - .artifacts/*.whl

variables:
  package_wheel_path:
    description: Caminho do wheel sincronizado no workspace para os jobs serverless.
    default: ${workspace.file_path}/.artifacts/pokeapi_lakehouse-0.9.5-py3-none-any.whl
  catalog:
    description: Unity Catalog que armazenará os schemas do projeto.
    default: workspace
  bronze_schema:
    description: Schema da camada Bronze.
    default: pokeapi_bronze_dev
  silver_schema:
    description: Schema da camada Silver.
    default: pokeapi_silver_dev
  gold_schema:
    description: Schema da camada Gold.
    default: pokeapi_gold_dev

workspace:
  host: https://dbc-147a7d81-86aa.cloud.databricks.com

experimental:
  # Os schemas já possuem sufixo de ambiente; não aplique também o prefixo automático
  # calculado por `mode: development`.
  skip_name_prefix_for_schema: true

targets:
  dev:
    default: true
    mode: development
    workspace:
      root_path: /Workspace/Users/${workspace.current_user.userName}/.bundle/${bundle.name}/${bundle.target}
